{
  "name": "@remote-codex/supervisor-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts src/worker-index.ts --format esm --platform node --external puppeteer-core --dts",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.3",
    "@fastify/websocket": "^11.0.2",
    "@homebridge/node-pty-prebuilt-multiarch": "^0.13.1",
    "better-sqlite3": "^12.10.0",
    "fastify": "^5.2.1",
    "marked": "^18.0.3",
    "puppeteer-core": "^24.43.1",
    "zod": "^3.24.2"
  }
}
