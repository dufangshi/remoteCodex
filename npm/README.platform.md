# Remote Codex native binary

This package contains the platform-specific native executable used by the `remote-codex` npm package. Install `remote-codex` instead of depending on this package directly.
