{
  "name": "@remote-codex/acp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --platform node --dts --external cross-spawn",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^1.4.0",
    "smol-toml": "^1.8.0"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
