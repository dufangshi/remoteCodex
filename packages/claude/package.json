{
  "name": "@remote-codex/claude",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --platform node --dts",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.198"
  }
}
